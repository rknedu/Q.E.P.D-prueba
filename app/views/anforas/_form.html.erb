<%= form_for(anfora) do |f| %>
<% if anfora.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(anfora.errors.count, "error") %> prohibited this anfora from being saved:</h2>

      <ul>
      <% anfora.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  

      <div class="form-group">
         <%= f.label :anfora_y_cobre, "Anfora y Cobre:" %>
         <%= f.text_field :anfora_y_cobre, class:"form-control" %>  
      </div>
      <div class="form-group">
         <%= f.label :anfora_cobre, "Anfora Cobre:" %>
         <%= f.text_field :anfora_cobre, class:"form-control" %>
      </div>
      <div class="actions">
         <%= f.submit "Crear Anfora", class:"btn btn-success" %>
      </div> 


  
<% end %>
