<%= form_for(formas_de_pago) do |f| %>
  <% if formas_de_pago.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(formas_de_pago.errors.count, "error") %> prohibited this formas_de_pago from being saved:</h2>

      <ul>
      <% formas_de_pago.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :nombre, "Nombre:" %>
    <%= f.text_field :nombre, class:"form-control" %>  
  </div>

  <div class="actions">
    <%= f.submit "Crear Formas de Pago", :class => "btn btn-success" %>
  </div>
<% end %>
