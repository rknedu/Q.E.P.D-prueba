<%= form_for(type_publication) do |f| %>
  <% if type_publication.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(type_publication.errors.count, "error") %> prohibited this type_publication from being saved:</h2>

      <ul>
      <% type_publication.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %>
    <%= f.text_field :nombre %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
