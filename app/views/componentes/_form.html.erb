<%= form_for(componente) do |f| %>
  <% if componente.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(componente.errors.count, "error") %> prohibited this componente from being saved:</h2>

      <ul>
      <% componente.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :nombre, "Nombre:" %>
    <%= f.text_field :nombre, class:"form-control" %>
  </div>

  <div class="form-group">
    <label>Servicio:</label>
    <%= f.collection_select :servicio_id, Servicio.all, :id, :nombre, {prompt: true}, {class: "form-control"} %>
    </div>

  <div class="actions">
    <%= f.submit "Crear Componente", :class => "btn btn-success" %>
  </div>
<% end %>
