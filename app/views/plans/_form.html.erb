<%= form_for(plan) do |f| %>
  <% if plan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(plan.errors.count, "error") %> prohibited this plan from being saved:</h2>

      <ul>
      <% plan.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <form role="form"> 
    <div class="form-group">
    <label>Nombre Funeraria:</label>
    <%= collection_select :plan, :empresa_id, Empresa.all, :id, :nombre, {prompt: true}, {class: "form-control"} %>
    </div>
    <div class="form-group">
    <label>Tipo de Plan:</label>
    <%= collection_select :plan, :type_plan_id, TypePlan.all, :id, :nombre, {prompt: true}, {class: "form-control"} %>
    </div>
    <div class="form-group">
    <label>Tipo de Cliente:</label>
    <%= collection_select :plan, :tipo_cliente_id, TipoCliente.all, :id, :nombre, {prompt: true}, {class: "form-control"} %>
    </div>

    <div class="form-group">
    <label>Servicio:</label>
    <%= collection_select :plan, :servicio_id, Servicio.all, :id, :nombre, {prompt: true}, {class: "form-control"} %>
    </div>

    <div class="form-group">
    <%= f.label :nombre, "Nombre de Plan:" %>
    <%= f.text_field :nombre, class:"form-control" %>
    </div>


    <div class="form-group">
      <%= f.label :precio, "Precio:" %>
      <%= f.number_field :precio, class:"form-control" %>
    </div>


    <div class="actions">
      <%= f.submit "Crear Plan", :class => "btn btn-success" %>
    </div>    
  </form>

  <% end %>

  

