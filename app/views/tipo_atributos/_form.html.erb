<%= form_for(tipo_atributo) do |f| %>
  <% if tipo_atributo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tipo_atributo.errors.count, "error") %> prohibited this tipo_atributo from being saved:</h2>

      <ul>
      <% tipo_atributo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :nombre, "Nombre:" %>
    <%= f.text_field :nombre, class:"form-control" %>
  </div>

  <div class="actions">
    <%= f.submit "Crear Tipo Atributo", :class => "btn btn-success" %>
  </div>
<% end %>
