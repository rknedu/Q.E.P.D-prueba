<%= form_for(componentes_plan) do |f| %>
  <% if componentes_plan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(componentes_plan.errors.count, "error") %> prohibited this componentes_plan from being saved:</h2>

      <ul>
      <% componentes_plan.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <label>Componente:</label>
    <%= f.collection_select :componente_id, Componente.all, :id, :nombre, {prompt: true}, {class: "form-control"} %>
  </div>

 <div class="form-group">
    <label>Plan:</label>
    <%= f.collection_select :plan_id, Plan.all, :id, :nombre, {prompt: true}, {class: "form-control"} %>
  </div>

  <div class="actions">
    <%= f.submit "Crear Componente Plan", :class => "btn btn-success" %>
  </div>
<% end %>

